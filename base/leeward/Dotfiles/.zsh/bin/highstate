#!/usr/bin/env zsh
#BIN="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN=${0:a:h}

clear
echo $blue"Applying local highstate from $HOME/salt..."
$BIN/local_salt state.highstate --state-output=mixed -l quiet $* | grep -v 'Result: Clean'
